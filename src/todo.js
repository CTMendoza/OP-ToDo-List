//this component creates new todos objects based on user entries on the web app

class todos {
    constructor (title, description,dueDate, priority) {
        this.title = title;
        this.description = description;
        this.dueDate = dueDate;
        this.priority = priority;
    }
}